import AccountTitle from "@/app/_components/AccountTitle";

export const metadata = {
	title: "Phone Number",
};

function page() {
	return (
		<div>
			<AccountTitle desc='You accualy Phone Number' title='Mobile Phone' />
		</div>
	);
}

export default page;
